{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'cd \"$(git rev-parse --show-toplevel)\" && bash scripts/agent-lifecycle.sh preflight --brief'"
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Bash|apply_patch",
        "hooks": [
          {
            "type": "command",
            "command": "sh -c 'cd \"$(git rev-parse --show-toplevel)\" || exit 2; command -v python3 >/dev/null 2>&1 || { echo \"agent-lifecycle guard: python3 not found; failing closed\" >&2; exit 2; }; exec python3 scripts/agent-lifecycle-guard.py'"
          }
        ]
      }
    ]
  }
}
